import React from 'react';
import '../../styles/base.css';

const Logo = () => {
    return (
        <div className="logo">
            <span className="logo-text">TB</span>
        </div>
    );
};

export default Logo;